# Code for diagram from http://www.websequencediagrams.com/

participant "Django application" as app
participant "Connector (in)" as con1
participant "Connector (out)" as con2
participant "Uniform interface" as int 


note over con1,con2: Django ROA
app->con1: Model.objects.filter(args)
con1->con2: Python → URL
con2->int: HTTP request (GET, POST, etc)
note right of int: Repository (black box) :\n1. SQL request\n2. Serialization\nor\n1. Get from cache
int->con2: Serialized objects (JSON, XML, etc)
con2->con1: Python ← Deserialization

con1->app: [<obj1>, <obj2>, etc]
