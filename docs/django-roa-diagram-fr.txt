# Code for diagram from http://www.websequencediagrams.com/

participant "Application Django" as app
participant "Connecteur (entrée)" as con1
participant "Connecteur (sortie)" as con2
participant "Interface uniforme" as int 


note over con1,con2: Django ROA
app->con1: Model.objects.filter(args)
con1->con2: Python → URL
con2->int: Requête HTTP (GET, POST, etc)
note right of int: Entrepôt (boîte noire) :\n1. Requête en base\n2. Sérialisation\nou\n1. Récupération en cache
int->con2: Objets sérialisés (JSON, XML, etc)
con2->con1: Python ← Déserialisation

con1->app: [<obj1>, <obj2>, etc]
